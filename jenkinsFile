import hudson.model.*
import jenkins.model.*

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Ingresando Datos Suma'
            }
        }
        stage('Clone Repository') {
            steps {
                git branch: 'main', url: 'https://github.com/Yahir2005/Pipeline_Suma.git'
            }
        }
         stage('Compile Java') {
            steps {
                script {
                    bat "javac Operaciones.java" 
                }
                script {

                    bat "java Operaciones 3 5"
                }
            }
        }
    }
}